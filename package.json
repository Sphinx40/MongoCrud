{
  "name": "mongo",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "heroku-postbuild": "YARN_PRODUCTION=false yarn install client && yarn build client"
  },
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.2",
    "nodemon": "^2.0.2"
  }
}
